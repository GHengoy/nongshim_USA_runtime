# Python-generated files
__pycache__/
*.py[oc]
build/
wheels/
*.egg-info

# Virtual environments
.venv

# Data and model weights (too large or runtime-generated)
data/
weights/
*.pt
*.pth

# macOS system files
.DS_Store

# IDE and temporary files
.vscode/
.idea/
*.swp
*.swo

# Node.js
node_modules/

# Python dist (setup.py 빌드 산출물)
# frontend/dist/ 는 포함 (빌드된 React 앱 — 다른 PC에서 npm 없이 바로 사용)

# Project-specific
only_image/
sample_Ansung_factory/
image.png
plan.md

# Worker runtime files (keep config.json and run_local.py only)
workers/*/data/
workers/*/weights/
workers/*/*.pfs
*.bak
*.db

# Secrets and local configuration
.env
.env.local

# Runtime configurations (auto-generated per deployment)
# Note: workers/*/config.json and configs/lines.json are now tracked
configs/lines.json
memory.md
